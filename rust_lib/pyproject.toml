[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "rust_lib"
version = "0.1.0"
description = "Rust-powered Jaccard similarity module for Python"
requires-python = ">=3.9"